# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=scheduler
# DB_PASSWORD is required and must be set
DB_PASSWORD=password
DB_NAME=scheduler
DB_SSLMODE=disable

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
